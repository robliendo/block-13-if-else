// -----------  Workshop If / Else   -------------
// Create a JS solution using if/else statements in JavaScript code in a script tag that follows the following rules.

// If the value is truthy, print true; 
const truthy = true;
    if(truthy){
        console.log(truthy === true);
    // If the value is falsy, print the corresponding result:
    } else {
        console.log(falsy === false);
    }
// "The boolean value false is falsy";
const booleanTrue = true;
const booleanFalsy = false;
    if(booleanTrue){
        console.log(booleanTrue === true);
    } else {
        console.log(booleanFalsy === false);
    }

// "The null value is falsy";
null === false;

const null = false;
    if(null){
        console.log(null === false);
    }

// "undefined is falsy";
undefined === false;

const undefined = false;
    if(undefined){
        console.log(undefined === false);
    }
// "The number 0 is falsy (the only falsy number)";

console.log(0 === false);

const 0 = false;
    if(0){
        console.log(0 === false);
    }
// "The empty string is falsy (the only falsy string)";
console.log("" === false);

const "" = false;
    if(""){
        console.log("" === false);
    }

// LETS try this again

// If the value is truthy, print true; 
const truthy = true;
    if(truthy){
        console.log("I am a string!");
    // If the value is falsy, print the corresponding result:
    } else {
        console.log("The boolean value false is falsy");
    }
// "The boolean value false is falsy";
const booleanTrue = true;
const booleanFalsy = false;
    if(booleanTrue){
        console.log(booleanTrue === true);
    } else {
        console.log(booleanFalsy === false);
    }

// "The null value is falsy";

console.log("The null value is falsy");

const null = false;
    if(null){
        console.log("The null value is falsy");
    }

// "undefined is falsy";
const undefined = false;
if(undefined){
        console.log("undefined is falsy");
    }

    if(undefined){
        console.log(undefined === false);
    }
// "The number 0 is falsy (the only falsy number)";


const 0 = false;
    if(0){
        console.log("The number 0 is falsy (the only falsy number)");
    }

// "The empty string is falsy (the only falsy string)";
console.log("" === false);

const "" = false;
    if(""){
        console.log("The empty string is falsy (the only falsy string)");
    }

